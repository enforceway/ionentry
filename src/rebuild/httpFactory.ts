// import { WrappedHttp } from "./wrapped.http";
// import { XHRBackend, RequestOptions } from '@angular/http';
// import { Router } from "@angular/router";

// export function interceptorFactory(xhrBackend: XHRBackend, requestOptions: RequestOptions, router: Router){
//   let service = new WrappedHttp(xhrBackend, requestOptions, router);
//   // Add interceptors here with service.addInterceptor(interceptor)
//   return service;
// }